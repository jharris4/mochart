{
  "name": "mochart-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for the mochart charting library, the movalid validation library, and the mochart demo app",
  "private": true,
  "license": "BSD-3-Clause",
  "author": {
    "name": "Jon Harris",
    "url": "https://github.com/jharris4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jharris4/mochart.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "overrides": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "scripts": {
    "dev": "npm run dev -w mochart-demo",
    "build": "npm run build -w mochart-demo",
    "build:pages": "node scripts/build-pages.mjs",
    "build:libs": "npm run build -w movalid && npm run build -w mochart && npm run build -w mochart-react && npm run build -w mochart-svelte && npm run build -w mochart-vue && npm run build -w mochart-lit",
    "prepare": "npm run build:libs",
    "test": "npm test --workspaces --if-present",
    "test:e2e": "npm run test:e2e -w mochart-demo",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "jsdom": "^29.1.1",
    "playwright-core": "^1.61.1",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.9"
  }
}
